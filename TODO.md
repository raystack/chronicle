# TODO

## Default theme

- [x] Replace sidebar logo placeholder (`CubeIcon`) with configurable logo from `config.logo` (light/dark)
- [ ] Add optional `icon` field per content dir and API in `chronicle.yaml` for sidebar cell leading icons
- [ ] Add action slot in top navbar (e.g. "Open in ChatGPT") via `config.navigation.actions`
- [x] Sidebar nesting supports 2 levels; 3rd+ level items are ignored
